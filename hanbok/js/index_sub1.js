const menuOpen=document.querySelector('.menu-open');const menuClose=document.querySelector('.menu-close');const mobileMenu=document.querySelector('.mobile-menu');menuOpen.addEventListener('click',function(){mobileMenu.classList.add('active')});menuClose.addEventListener('click',function(){mobileMenu.classList.remove('active')});
    const tabLinks=document.querySelectorAll('.sub-tab-list a, .mobile-sub a');const sections=document.querySelectorAll('.sub-section');const pageName=document.getElementById('pageName');
    function showTab(id){const target=document.getElementById(id)||document.getElementById('intro');sections.forEach(sec=>sec.classList.toggle('active',sec===target));document.querySelectorAll('.sub-tab-list a').forEach(a=>a.classList.toggle('active',a.dataset.tab===target.id));const active=document.querySelector('.sub-tab-list a.active');if(active){pageName.textContent=active.textContent.trim()}mobileMenu.classList.remove('active')}
    tabLinks.forEach(link=>{link.addEventListener('click',function(e){const id=this.getAttribute('href').replace('#','');if(document.getElementById(id)){e.preventDefault();showTab(id);history.replaceState(null,'','#'+id);window.scrollTo({top:0,behavior:'smooth'})}})});
    if(location.hash){showTab(location.hash.replace('#',''))}