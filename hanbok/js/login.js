const menuOpen=document.querySelector('.menu-open');const menuClose=document.querySelector('.menu-close');const mobileMenu=document.querySelector('.mobile-menu');if(menuOpen){menuOpen.addEventListener('click',()=>mobileMenu.classList.add('active'))}if(menuClose){menuClose.addEventListener('click',()=>mobileMenu.classList.remove('active'))}

const tabs=document.querySelectorAll('.tab');const idPanel=document.getElementById('id-panel');const pwPanel=document.getElementById('pw-panel');function activeTab(name){tabs.forEach(t=>t.classList.toggle('active',t.dataset.tab===name));idPanel.classList.toggle('hidden',name!=='id');pwPanel.classList.toggle('hidden',name!=='pw');}tabs.forEach(t=>t.addEventListener('click',()=>activeTab(t.dataset.tab)));document.getElementById('idCheckBtn').addEventListener('click',()=>{document.querySelectorAll('.id-state').forEach(e=>e.classList.add('hidden'));document.getElementById('idResult').classList.remove('hidden')});document.getElementById('goPwBtn').addEventListener('click',()=>activeTab('pw'));document.querySelectorAll('[data-next]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.pw-state').forEach(e=>e.classList.add('hidden'));document.getElementById(btn.dataset.next).classList.remove('hidden')}));